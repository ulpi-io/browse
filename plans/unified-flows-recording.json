{
  "title": "Unified Flows, Recording, and Replay",
  "branch": "feat/unified-flows",
  "mode": "EXPANSION",
  "overview": "Wire flows, recording, chain, and replay together. Recording becomes the canonical capture model, all execution goes through executeCommand(), flows work on any AutomationTarget.",
  "scopeChallenge": "All pieces exist (executeCommand, lifecycle hooks, recording after-hook, flow parser, 3 export formats). We're connecting them, not building from scratch. EXPANSION mode for comprehensive test coverage of nested recording, app target flows, and edge cases.",
  "existingCodeLeverage": [
    { "subProblem": "Command execution pipeline", "existingCode": "src/automation/executor.ts", "action": "reuse" },
    { "subProblem": "Lifecycle hooks", "existingCode": "src/automation/events.ts", "action": "reuse" },
    { "subProblem": "Recording after-hook", "existingCode": "src/server.ts:314-333", "action": "reuse" },
    { "subProblem": "YAML flow parsing", "existingCode": "src/flow-parser.ts", "action": "reuse" },
    { "subProblem": "Browse chain export", "existingCode": "src/export/record.ts exportBrowse()", "action": "reuse" },
    { "subProblem": "Replay/Playwright export", "existingCode": "src/export/replay.ts", "action": "extend" },
    { "subProblem": "YAML serialization in flow save", "existingCode": "src/commands/meta/flows.ts:87-99", "action": "extend" },
    { "subProblem": "Config project root detection", "existingCode": "src/config.ts loadConfig()", "action": "extend" },
    { "subProblem": "Target capability gating", "existingCode": "src/automation/executor.ts:56-72", "action": "reuse" }
  ],
  "failureModes": [
    { "risk": "Circular imports: flows.ts -> executor.ts -> registry.ts -> index.ts -> flows.ts", "affectedTasks": ["TASK-005"], "mitigation": "Use dynamic await import() for executor in flows.ts" },
    { "risk": "Lifecycle hooks fire for both outer flow + inner steps", "affectedTasks": ["TASK-005", "TASK-007"], "mitigation": "skipRecording: true on flow/chain/retry/watch" },
    { "risk": "writeCapture closure conflict in nested execution", "affectedTasks": ["TASK-005"], "mitigation": "Sequential step execution means before/after hooks don't overlap" },
    { "risk": "App target gets browser-only export format", "affectedTasks": ["TASK-008"], "mitigation": "Guard at export time, not recording time" },
    { "risk": "findProjectRoot() returns null outside project", "affectedTasks": ["TASK-003", "TASK-010"], "mitigation": "Fall back to BROWSE_LOCAL_DIR or .browse/ — never /tmp" },
    { "risk": "Recursive flow creates infinite loop", "affectedTasks": ["TASK-005", "TASK-013"], "mitigation": "Depth counter with cap of 10 — rejects with clear error at depth 11" },
    { "risk": "Lifecycle not forwarded — sub-steps not recorded", "affectedTasks": ["TASK-002", "TASK-005", "TASK-006"], "mitigation": "Explicit lifecycle parameter forwarded to nested executeCommand() calls — not implicit, not inferred" }
  ],
  "testCoverageMap": [
    { "codepath": "Flow sub-step via executeCommand()", "coveringTask": "TASK-011", "testType": "integration" },
    { "codepath": "Lifecycle forwarding to sub-steps", "coveringTask": "TASK-011", "testType": "integration" },
    { "codepath": "Recording captures flow sub-steps", "coveringTask": "TASK-011", "testType": "integration" },
    { "codepath": "Chain via executeCommand()", "coveringTask": "TASK-012", "testType": "integration" },
    { "codepath": "record export flow round-trip", "coveringTask": "TASK-012", "testType": "integration" },
    { "codepath": "App target flow execution", "coveringTask": "TASK-011", "testType": "integration" },
    { "codepath": "replay/playwright export blocked on app", "coveringTask": "TASK-013", "testType": "integration" },
    { "codepath": "retry/watch browser-only gating", "coveringTask": "TASK-013", "testType": "integration" },
    { "codepath": "Nested flow recording", "coveringTask": "TASK-013", "testType": "integration" },
    { "codepath": "exportFlowYaml() output validity", "coveringTask": "TASK-012", "testType": "unit" },
    { "codepath": "findProjectRoot() edge cases", "coveringTask": "TASK-011", "testType": "unit" },
    { "codepath": "Recursive flow depth cap enforcement", "coveringTask": "TASK-013", "testType": "integration" }
  ],
  "tasks": [
    {
      "id": "TASK-001",
      "title": "Type lastRecording on Session and deduplicate RecordedStep",
      "description": "Remove _lastRecording property-bag hack. Add typed lastRecording field to Session interface in src/session/manager.ts. Deduplicate RecordedStep — import from src/export/record.ts, re-export from src/session/manager.ts. Replace all (currentSession as any)._lastRecording in src/commands/meta/recording.ts and src/commands/meta/flows.ts.",
      "type": "refactor",
      "effort": "S",
      "priority": "P0",
      "dependsOn": [],
      "acceptanceCriteria": [
        "Session interface has lastRecording: RecordedStep[] | null field",
        "Zero occurrences of (currentSession as any)._lastRecording in codebase",
        "RecordedStep imported from src/export/record.ts, re-exported from src/session/manager.ts"
      ],
      "filesToModify": ["src/session/manager.ts", "src/commands/meta/recording.ts", "src/commands/meta/flows.ts"],
      "filesToCreate": [],
      "agent": "nodejs-cli-senior-engineer"
    },
    {
      "id": "TASK-002",
      "title": "Add lifecycle field to CommandContext and thread it in transports",
      "description": "Add lifecycle?: CommandLifecycle field to CommandContext in src/automation/command.ts. Update src/server.ts to include the lifecycle object in the context passed to executeCommand(), so that command definitions receive ctx.lifecycle and can forward it to nested executeCommand() calls as opts.lifecycle. This is the mechanism that makes sub-step recording work. Also update src/mcp/server.ts to pass its lifecycle into context for forward-compatibility.",
      "type": "feature",
      "effort": "S",
      "priority": "P0",
      "dependsOn": [],
      "acceptanceCriteria": [
        "CommandContext has lifecycle?: CommandLifecycle field",
        "src/server.ts passes lifecycle into context: context: { ..., lifecycle }",
        "src/mcp/server.ts also passes its lifecycle into context (forward-compat)",
        "npx tsc --noEmit passes"
      ],
      "filesToModify": ["src/automation/command.ts", "src/server.ts", "src/mcp/server.ts"],
      "filesToCreate": [],
      "agent": "nodejs-cli-senior-engineer"
    },
    {
      "id": "TASK-003",
      "title": "Export findProjectRoot() from config",
      "description": "Extract the project-root detection logic from loadConfig() in src/config.ts into a separate exported findProjectRoot() function. loadConfig() calls it internally. Flow and recording commands use it to resolve flowPaths relative to project root.",
      "type": "refactor",
      "effort": "S",
      "priority": "P0",
      "dependsOn": [],
      "acceptanceCriteria": [
        "findProjectRoot() exported from src/config.ts, returns string | null",
        "loadConfig() uses findProjectRoot() internally with same behavior",
        "No change to existing loadConfig() return values"
      ],
      "filesToModify": ["src/config.ts"],
      "filesToCreate": [],
      "agent": "nodejs-cli-senior-engineer"
    },
    {
      "id": "TASK-004",
      "title": "Widen meta dispatch appMetaCommands for flow/chain/record/sessions",
      "description": "In src/commands/meta/index.ts, expand appMetaCommands set at line 104 to include flow, chain, doctor, sessions, session-close, record. sessions and session-close are safe — they use sessionManager, not bm. Do NOT include state (browser storage — uses getContext()), retry, watch (getPage()), har, video (browser API), handoff, resume (browser-only). Change handleMetaCommand signature from bm: BrowserTarget to target: AutomationTarget. Thread lifecycle?: CommandLifecycle parameter to handleFlowsCommand and handleSystemCommand. The sessions.ts handler signature stays BrowserTarget — its bm parameter is only used by state/handoff/resume cases, and meta dispatch passes it via cast.",
      "type": "feature",
      "effort": "M",
      "priority": "P1",
      "dependsOn": ["TASK-002"],
      "acceptanceCriteria": [
        "--platform android --app com.example flow <file> reaches handleFlowsCommand without 'not available' error",
        "sessions and session-close work on app targets (they use sessionManager, not bm)",
        "state save, har start, video start still blocked for app targets with clear error",
        "handleMetaCommand accepts AutomationTarget — browser sub-handlers cast internally"
      ],
      "filesToModify": ["src/commands/meta/index.ts"],
      "filesToCreate": [],
      "agent": "nodejs-cli-senior-engineer"
    },
    {
      "id": "TASK-005",
      "title": "Make flows target-neutral + route through executeCommand()",
      "description": "In src/commands/meta/flows.ts, change bm: BrowserTarget to target: AutomationTarget. Accept lifecycle?: CommandLifecycle parameter. Replace all direct handleReadCommand/handleWriteCommand/handleMetaCommand dispatch with executeCommand() calls that explicitly forward lifecycle: executeCommand(step.command, step.args, { context: { ..., lifecycle }, lifecycle }). Extract shared executeFlowSteps() helper. Recursive flow rule: flow run inside a flow allowed with max depth 10, tracked via depth parameter on executeFlowSteps() (default 0, incremented per nested call — NOT module-level state, which is unsafe in multi-session server). Rejects with 'flow nesting depth exceeded (max 10)'. For retry and watch: add runtime guard for non-browser targets.",
      "type": "feature",
      "effort": "L",
      "priority": "P1",
      "dependsOn": ["TASK-001", "TASK-002", "TASK-004"],
      "acceptanceCriteria": [
        "Flow sub-steps go through executeCommand() with lifecycle explicitly forwarded",
        "flow run and flow <file> share executeFlowSteps() helper",
        "Nested flow run works up to depth 10, rejects at depth 11 with clear error",
        "retry and watch throw clear error on non-browser targets",
        "Existing flow YAML files execute identically"
      ],
      "filesToModify": ["src/commands/meta/flows.ts"],
      "filesToCreate": [],
      "agent": "nodejs-cli-senior-engineer"
    },
    {
      "id": "TASK-006",
      "title": "Make chain target-neutral + route through executeCommand()",
      "description": "In src/commands/meta/system.ts, change bm: BrowserTarget to target: AutomationTarget. Accept lifecycle?: CommandLifecycle parameter. In chain case, replace hardcoded WRITE_SET/READ_SET (lines 69-70) with executeCommand() calls that explicitly forward lifecycle: executeCommand(name, cmdArgs, { context: { ..., lifecycle }, lifecycle }). Keep policy check. Guard status/url with runtime check.",
      "type": "feature",
      "effort": "M",
      "priority": "P1",
      "dependsOn": ["TASK-002", "TASK-004"],
      "acceptanceCriteria": [
        "Chain sub-steps go through executeCommand() with lifecycle explicitly forwarded",
        "Hardcoded WRITE_SET/READ_SET removed — routing derived from registry",
        "status/url still work on browser targets, throw clear error on app targets"
      ],
      "filesToModify": ["src/commands/meta/system.ts"],
      "filesToCreate": [],
      "agent": "nodejs-cli-senior-engineer"
    },
    {
      "id": "TASK-007",
      "title": "Add skipRecording + targetSupport: browser to registry",
      "description": "In src/automation/registry.ts, add skipRecording: true to flow, chain, retry, watch specs. Mark retry and watch as targetSupport: 'browser' for proper capability errors on app targets.",
      "type": "feature",
      "effort": "S",
      "priority": "P1",
      "dependsOn": ["TASK-005"],
      "acceptanceCriteria": [
        "flow command itself not recorded when recording active — only sub-steps",
        "retry on Android session fails with 'not available for android-app targets' from executor",
        "chain command itself not recorded — sub-steps are"
      ],
      "filesToModify": ["src/automation/registry.ts"],
      "filesToCreate": [],
      "agent": "nodejs-cli-senior-engineer"
    },
    {
      "id": "TASK-008",
      "title": "Split recording into target-neutral and browser-only",
      "description": "In src/commands/meta/recording.ts, change bm: BrowserTarget to accept AutomationTarget. Guard har and video with browser-only check. Guard record export replay and playwright to reject for non-browser sessions. record start/stop/status and record export browse are target-neutral.",
      "type": "feature",
      "effort": "M",
      "priority": "P1",
      "dependsOn": ["TASK-001", "TASK-004"],
      "acceptanceCriteria": [
        "record start/stop works on Android sessions",
        "record export browse works on Android sessions",
        "record export replay on Android session -> 'replay export requires a browser session'",
        "har start on Android session -> 'HAR recording requires a browser session'"
      ],
      "filesToModify": ["src/commands/meta/recording.ts"],
      "filesToCreate": [],
      "agent": "nodejs-cli-senior-engineer"
    },
    {
      "id": "TASK-009",
      "title": "Add flow as a recording export format",
      "description": "Add exportFlowYaml(steps: RecordedStep[]): string to src/export/record.ts. Extract YAML serialization from flows.ts lines 87-99. Add flow format branch to record export in recording.ts. Update flow save to call exportFlowYaml(). Also update src/automation/registry.ts — the record command spec's MCP schema, CLI help text, and usage string currently only advertise browse|replay|playwright. Add flow to those metadata strings.",
      "type": "feature",
      "effort": "S",
      "priority": "P2",
      "dependsOn": ["TASK-001", "TASK-008"],
      "acceptanceCriteria": [
        "record export flow produces valid YAML that flow <file> can execute",
        "flow save uses exportFlowYaml() — no inline YAML generation",
        "Round-trip: record start -> commands -> record export flow -> flow <file> succeeds",
        "CLI help / MCP schema / usage string for record includes flow as a format option"
      ],
      "filesToModify": ["src/export/record.ts", "src/commands/meta/recording.ts", "src/commands/meta/flows.ts", "src/automation/registry.ts"],
      "filesToCreate": [],
      "agent": "nodejs-cli-senior-engineer"
    },
    {
      "id": "TASK-010",
      "title": "Fix flows dir to use config-root-aware resolution",
      "description": "Replace BROWSE_LOCAL_DIR || '/tmp' in src/commands/meta/flows.ts with proper resolution: (1) browse.json flowPaths resolved relative to project root via findProjectRoot(), (2) BROWSE_LOCAL_DIR/flows, (3) .browse/flows as last resort. Remove /tmp fallback.",
      "type": "feature",
      "effort": "S",
      "priority": "P2",
      "dependsOn": ["TASK-003", "TASK-005"],
      "acceptanceCriteria": [
        "flow save myflow stores to <project-root>/.browse/flows/myflow.yaml",
        "browse.json with flowPaths resolves relative to project root",
        "No flows saved to /tmp when project root exists"
      ],
      "filesToModify": ["src/commands/meta/flows.ts"],
      "filesToCreate": [],
      "agent": "nodejs-cli-senior-engineer"
    },
    {
      "id": "TASK-011",
      "title": "Integration tests — flow execution through executor",
      "description": "Test that flows route through executeCommand(), lifecycle hooks fire, and recording captures sub-steps. Test both browser and mock app targets. Test nested flow doesn't create duplicate recordings. Include unit test for findProjectRoot() edge cases (no .git, nested projects).",
      "type": "test",
      "effort": "L",
      "priority": "P2",
      "dependsOn": ["TASK-005", "TASK-007"],
      "acceptanceCriteria": [
        "Test: record start -> flow run -> record stop -> recording contains individual sub-steps",
        "Test: flow with unsupported command on app target -> fails with capability error at step",
        "Test: nested flow doesn't create duplicate recordings",
        "Test: findProjectRoot() returns null outside project, finds root with .git"
      ],
      "filesToModify": [],
      "filesToCreate": ["test/unified-flows.test.ts"],
      "agent": "nodejs-cli-senior-engineer"
    },
    {
      "id": "TASK-012",
      "title": "Integration tests — chain through executor + export formats",
      "description": "Test chain dispatch through executeCommand(), recording of chain sub-steps, and export format restrictions.",
      "type": "test",
      "effort": "M",
      "priority": "P2",
      "dependsOn": ["TASK-006", "TASK-007", "TASK-008", "TASK-009"],
      "acceptanceCriteria": [
        "Test: chain on browser session — sub-steps recorded individually",
        "Test: record export flow produces valid YAML -> flow re-executes",
        "Test: record export replay on non-browser session -> rejects with clear error"
      ],
      "filesToModify": [],
      "filesToCreate": ["test/unified-flows.test.ts"],
      "agent": "nodejs-cli-senior-engineer"
    },
    {
      "id": "TASK-013",
      "title": "Edge case tests — recursive flows, depth cap, app targets",
      "description": "Test edge cases: recursive flow execution depth cap (allowed up to 10, rejects at 11 with 'flow nesting depth exceeded'), retry/watch browser-only gating, export format restrictions on app sessions.",
      "type": "test",
      "effort": "M",
      "priority": "P2",
      "dependsOn": ["TASK-007", "TASK-008", "TASK-011"],
      "acceptanceCriteria": [
        "Test: nested flow run at depth 2 -> succeeds, sub-steps recorded",
        "Test: flow run at depth 11 -> rejects with 'flow nesting depth exceeded'",
        "Test: retry on app target -> 'not available for android-app targets' from executor",
        "Test: record export playwright on app session -> rejects, record export browse -> succeeds"
      ],
      "filesToModify": [],
      "filesToCreate": ["test/unified-flows.test.ts"],
      "agent": "nodejs-cli-senior-engineer"
    },
    {
      "id": "TASK-014",
      "title": "Update docs — README workflow commands section",
      "description": "Update README workflow commands section to document record export flow, flow execution on app targets, browser-only commands. Update CHANGELOG.",
      "type": "docs",
      "effort": "S",
      "priority": "P3",
      "dependsOn": ["TASK-009", "TASK-011"],
      "acceptanceCriteria": [
        "README documents record export flow format",
        "README notes which workflow commands are browser-only",
        "CHANGELOG has entry for unified flows/recording"
      ],
      "filesToModify": ["README.md", "CHANGELOG.md"],
      "filesToCreate": [],
      "agent": "general-purpose"
    },
    {
      "id": "TASK-015",
      "title": "Update CLAUDE.md reference docs",
      "description": "Update architecture.md and exports-reference.md to reflect lifecycle threading, exportFlowYaml, findProjectRoot, widened appMetaCommands, CommandContext.lifecycle field.",
      "type": "docs",
      "effort": "S",
      "priority": "P3",
      "dependsOn": ["TASK-005", "TASK-006"],
      "acceptanceCriteria": [
        "Architecture doc shows lifecycle threading through flows/chain",
        "Exports reference lists exportFlowYaml, findProjectRoot",
        "Command context docs mention lifecycle field"
      ],
      "filesToModify": [".claude/claude-md-refs/architecture.md", ".claude/claude-md-refs/exports-reference.md"],
      "filesToCreate": [],
      "agent": "general-purpose"
    }
  ],
  "dependencies": {
    "TASK-001": [],
    "TASK-002": [],
    "TASK-003": [],
    "TASK-004": ["TASK-002"],
    "TASK-005": ["TASK-001", "TASK-002", "TASK-004"],
    "TASK-006": ["TASK-002", "TASK-004"],
    "TASK-007": ["TASK-005"],
    "TASK-008": ["TASK-001", "TASK-004"],
    "TASK-009": ["TASK-001", "TASK-008"],
    "TASK-010": ["TASK-003", "TASK-005"],
    "TASK-011": ["TASK-005", "TASK-007"],
    "TASK-012": ["TASK-006", "TASK-007", "TASK-008", "TASK-009"],
    "TASK-013": ["TASK-007", "TASK-008", "TASK-011"],
    "TASK-014": ["TASK-009", "TASK-011"],
    "TASK-015": ["TASK-005", "TASK-006"]
  }
}
